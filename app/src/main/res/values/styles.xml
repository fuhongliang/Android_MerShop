<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="NiceDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="DefaultAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/enter_anim</item>
        <item name="android:windowExitAnimation">@anim/exit_anim</item>
    </style>

    <style name="Bottom_top_dialog_style" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/vt_popup_bottom_to_top</item>
    </style>

    <style name="vt_popup_bottom_to_top">
        <item name="android:windowEnterAnimation">@anim/vt_change_room_slide_up</item>
        <item name="android:windowExitAnimation">@anim/vt_change_room_slide_down</item>
    </style>

    <style name="loadDialog" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- RVPIndicator -->
    <declare-styleable name="RVPIndicator">
        <attr name="text_color_normal" format="color" />
        <attr name="text_color_selected" format="color" />
        <attr name="text_size" format="dimension" />
        <!--指示器可见数量-->
        <attr name="indicator_visible_count" format="integer" />
        <attr name="indicator_color" format="color" />
        <attr name="indicator_style">
            <enum name="bitmap" value="0" />
            <enum name="line" value="1" />
            <enum name="triangle" value="2" />
        </attr>
        <!--指示器bitmap资源,style=bitmap有效-->
        <attr name="style_bitmap_src" format="reference" />
        <!--线性指示器左右padding，默认根据数量均分屏幕-->
        <attr name="style_line_padding" format="dimension" />
        <!--指示器高度-->
        <attr name="indicator_height" format="dimension" />
        <!--指示器宽度-->
        <attr name="indicator_width" format="dimension" />
    </declare-styleable>

</resources>
